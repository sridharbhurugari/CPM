import { GlobalDispenseSyncRequest } from './global-dispense-sync-request';

describe('GlobalDispenseSyncRequest', () => {
  it('should create an instance', () => {
    expect(new GlobalDispenseSyncRequest()).toBeTruthy();
  });
});
