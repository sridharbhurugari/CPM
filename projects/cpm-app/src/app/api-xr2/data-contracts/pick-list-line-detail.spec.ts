import { PickListLineDetail } from './pick-list-line-detail';

describe('PickListLineDetail', () => {
  it('should create an instance', () => {
    expect(new PickListLineDetail()).toBeTruthy();
  });
});
