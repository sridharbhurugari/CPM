import { GridMultiSelectDirective } from './grid-multi-select.directive';

describe('GridMultiSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new GridMultiSelectDirective();
    expect(directive).toBeTruthy();
  });
});
