import { GridReorderDirective } from './grid-reorder.directive';

describe('GridReorderDirective', () => {
  it('should create an instance', () => {
    const directive = new GridReorderDirective();
    expect(directive).toBeTruthy();
  });
});
