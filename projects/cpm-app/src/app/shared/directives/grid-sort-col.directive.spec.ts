import { GridSortColDirective } from './grid-sort-col.directive';

describe('GridSortColDirective', () => {
  it('should create an instance', () => {
    const directive = new GridSortColDirective();
    expect(directive).toBeTruthy();
  });
});
