import { Component, Input } from '@angular/core';

@Component({
    selector: 'app-header-title-bottom-margin-container',
    template: ''
})
export class MockHeaderTitleBottomMarginContainerComponent {
    @Input()
    title: string;
}