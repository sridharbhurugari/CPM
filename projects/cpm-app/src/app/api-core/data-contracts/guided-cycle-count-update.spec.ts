import { deviceCycleCountItemUpdate } from './guided-cycle-count-update';
import { TestBed } from '@angular/core/testing';

describe('GuidedCycleCountUpdate', () => {
  it('should create an instance', () => {
    const component:deviceCycleCountItemUpdate = TestBed.get(deviceCycleCountItemUpdate);
    expect(component).toBeTruthy();
  });
});
