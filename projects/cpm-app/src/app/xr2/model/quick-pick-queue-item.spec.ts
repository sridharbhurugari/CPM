import { QuickPickQueueItem } from './quick-pick-queue-item';

describe('QuickPickQueueItem', () => {
  it('should create an instance', () => {
    expect(new QuickPickQueueItem()).toBeTruthy();
  });
});
