import { QuickPickDrawerData } from './quick-pick-drawer-data';

describe('PicklistQueueItem', () => {
  it('should create an instance', () => {
    expect(new QuickPickDrawerData(null)).toBeTruthy();
  });
});
