import { QuickPickRobotDispenseBoxItem } from './quick-pick-robot-dispense-box-item';

describe('PicklistQueueItem', () => {
  it('should create an instance', () => {
    expect(new QuickPickRobotDispenseBoxItem(null)).toBeTruthy();
  });
});
