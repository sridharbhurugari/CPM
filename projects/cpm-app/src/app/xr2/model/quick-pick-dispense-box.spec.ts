import { QuickPickDispenseBox } from './quick-pick-dispense-box';

describe('PicklistQueueItem', () => {
  it('should create an instance', () => {
    expect(new QuickPickDispenseBox(null)).toBeTruthy();
  });
});
